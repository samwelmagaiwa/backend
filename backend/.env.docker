# Backend .env.docker
# --------------------
# This file is reserved for **Docker infrastructure only** (if needed).
# Do NOT put Laravel app configuration here (no APP_, DB_, CORS_, SMS_, etc.).
#
# The backend application configuration should live in:
#   - .env (local/dev)
#   - .env.production (internal MNH deployment)
#   - .env.docker.dev (Docker dev environment, used by docker-compose.yml)
#
# Currently left empty on purpose.
